<?php
f( $S_SERVER["REQUESR_METHOD"]=="POST"){
    $nombre=htmlspecialchars($_POST[nombre]);
    $email=htmlspecialchars($_POST[email]);
    $mensaje=htmlspecialchars($_POST[mensaje]);
    
    echo "Gracias $nombre, por su mensaje";
    }
    ?>